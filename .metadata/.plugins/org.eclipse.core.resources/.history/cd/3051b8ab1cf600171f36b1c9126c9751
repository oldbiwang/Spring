package test;

import save.Save;

public class Test {
	private Save save = new SaveDB();
	
	public SaveDB getSaveDB() {
		return saveDB;
	}
	
	public void setSaveDB(SaveDB saveDB) {
		this.saveDB = saveDB;
	}
	
	public static void main(String[] args) {
		Test test = new Test();
		test.getSaveDB().save();
	}
}
